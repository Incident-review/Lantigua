'use client'

import { useState } from 'react'
import Image from 'next/image'
import Link from 'next/link'
import { ArrowUpRight, CalendarDays, Coffee, MapPin, Menu, TreePine, Users, X } from 'lucide-react'
import { events } from '@/data/events'

const navItems = [['La casa', '#casa'], ['La experiencia', '#experiencia'], ['Eventos', '#eventos']]

export function LantiguaHeader() {
  const [open, setOpen] = useState(false)
  return <header className="absolute inset-x-0 top-0 z-20 border-b border-primary-foreground/20 text-primary-foreground">
    <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5 lg:px-10">
      <Link href="#inicio" className="font-serif text-2xl tracking-wide">lantigua<span className="text-accent">.</span></Link>
      <nav className="hidden items-center gap-8 text-sm lg:flex">{navItems.map(([label, href]) => <Link key={href} href={href} className="transition-opacity hover:opacity-70">{label}</Link>)}<Link href="#reservas" className="rounded-full bg-primary-foreground px-5 py-2.5 font-medium text-primary transition-colors hover:bg-accent">Reservar estancia <ArrowUpRight className="ml-1 inline size-4" /></Link></nav>
      <button aria-label={open ? 'Cerrar menú' : 'Abrir menú'} className="lg:hidden" onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button>
    </div>
    {open && <nav className="flex flex-col gap-5 bg-primary px-6 pb-6 text-sm lg:hidden">{navItems.map(([label, href]) => <Link key={href} href={href} onClick={() => setOpen(false)}>{label}</Link>)}<Link href="#reservas" onClick={() => setOpen(false)}>Reservar estancia <ArrowUpRight className="ml-1 inline size-4" /></Link></nav>}
  </header>
}

export function Hero() {
  return <section id="inicio" className="relative flex min-h-[680px] items-end overflow-hidden bg-primary text-primary-foreground lg:min-h-[780px]">
    <Image src="/lantigua-hero.png" alt="Casa rural Lantigua entre encinas" fill priority className="object-cover" />
    <div className="absolute inset-0 bg-gradient-to-t from-primary/85 via-primary/20 to-primary/20" />
    <LantiguaHeader />
    <div className="relative mx-auto w-full max-w-6xl px-6 pb-16 lg:px-10 lg:pb-24">
      <p className="mb-6 text-xs font-semibold uppercase tracking-[0.28em] text-accent">Valle de los Pedroches · Córdoba</p>
      <h1 className="max-w-3xl font-serif text-6xl leading-[0.95] tracking-tight text-balance md:text-8xl">Una casa para volver a lo esencial.</h1>
      <div className="mt-8 flex flex-col gap-5 sm:flex-row sm:items-center"><Link href="#reservas" className="inline-flex w-fit items-center rounded-full bg-accent px-6 py-3.5 text-sm font-semibold text-primary transition-transform hover:scale-[1.02]">Comprueba disponibilidad <ArrowUpRight className="ml-2 size-4" /></Link><p className="max-w-xs text-sm leading-relaxed text-primary-foreground/75">Piedra, encinas y tiempo para no hacer nada. Bienvenidos a Lantigua.</p></div>
    </div>
  </section>
}

export function HouseStory() {
  return <section id="casa" className="bg-background px-6 py-24 lg:px-10 lg:py-32"><div className="mx-auto grid max-w-6xl gap-14 lg:grid-cols-[0.9fr_1.1fr] lg:items-center lg:gap-24">
    <div><p className="eyebrow">La casa</p><h2 className="section-title">El lujo de ir<br /><em>sin prisa.</em></h2><p className="mt-7 max-w-md text-base leading-7 text-muted-foreground">Lantigua es una casa rural restaurada con respeto por lo que ya estaba aquí. Un refugio de piedra y madera, rodeado por la calma de la dehesa.</p><Link href="#reservas" className="mt-8 inline-flex items-center border-b border-primary pb-2 text-sm font-semibold">Conoce Lantigua <ArrowUpRight className="ml-2 size-4" /></Link></div>
    <div className="relative aspect-[4/3] overflow-hidden rounded-[2rem]"><Image src="/lantigua-interior.png" alt="Salón de Lantigua con chimenea" fill className="object-cover" /><div className="absolute bottom-5 left-5 rounded-full bg-background/90 px-4 py-2 text-xs font-medium text-foreground">Casa completa · 8 huéspedes</div></div>
  </div></section>
}

export function Experience() {
  const details = [{ icon: TreePine, title: 'La dehesa', text: 'Senderos que empiezan en la puerta' }, { icon: Coffee, title: 'La sobremesa', text: 'Cocina local y mañanas sin alarma' }, { icon: Users, title: 'La casa', text: 'Espacio para compartir y descansar' }]
  return <section id="experiencia" className="bg-secondary px-6 py-24 lg:px-10 lg:py-28"><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="eyebrow">La experiencia</p><h2 className="section-title max-w-2xl">Ven por el paisaje.<br /><em>Quédate por la calma.</em></h2></div><p className="max-w-xs text-sm leading-6 text-muted-foreground">Todo lo que necesitas para sentirte lejos, sin estarlo.</p></div><div className="mt-14 grid gap-px overflow-hidden rounded-2xl bg-border md:grid-cols-3">{details.map(({ icon: Icon, title, text }) => <div key={title} className="bg-background p-8"><Icon className="size-7 text-accent-foreground" strokeWidth={1.5} /><h3 className="mt-12 font-serif text-2xl">{title}</h3><p className="mt-2 text-sm text-muted-foreground">{text}</p></div>)}</div></div></section>
}

export function Booking() {
  return <section id="reservas" className="bg-primary px-6 py-24 text-primary-foreground lg:px-10 lg:py-28"><div className="mx-auto grid max-w-6xl gap-12 lg:grid-cols-[0.75fr_1.25fr] lg:items-center"><div><p className="eyebrow text-accent">Tu estancia</p><h2 className="section-title text-primary-foreground">Reserva tu<br /><em>momento.</em></h2><p className="mt-6 max-w-sm text-sm leading-6 text-primary-foreground/70">Consulta fechas y disponibilidad en nuestro motor de reservas seguro.</p><div className="mt-8 flex items-center gap-3 text-sm text-primary-foreground/80"><CalendarDays className="size-5 text-accent" /> Reserva directa con Mr Plan</div></div><div className="rounded-2xl bg-background p-6 text-foreground sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-widest text-muted-foreground">Disponibilidad</p><h3 className="mt-2 font-serif text-3xl">Encuentra tus fechas</h3></div><div className="rounded-full bg-secondary px-3 py-1 text-[11px] font-medium">Mr Plan</div></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><label className="flex flex-col gap-2 text-xs font-semibold uppercase tracking-wide text-muted-foreground">Entrada<input type="date" className="rounded-lg border border-border bg-background px-3 py-3 text-sm font-normal tracking-normal text-foreground" /></label><label className="flex flex-col gap-2 text-xs font-semibold uppercase tracking-wide text-muted-foreground">Salida<input type="date" className="rounded-lg border border-border bg-background px-3 py-3 text-sm font-normal tracking-normal text-foreground" /></label></div><div className="mt-4 flex items-center justify-between border-t border-border pt-5"><p className="text-xs text-muted-foreground">Widget listo para conectar con tu cuenta Mr Plan.</p><a href="https://mrplan.es" target="_blank" rel="noreferrer" className="rounded-full bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground transition-opacity hover:opacity-85">Ver disponibilidad <ArrowUpRight className="ml-1 inline size-4" /></a></div></div></div></section>
}

export function EventsFeed() {
  const [selectedEvent, setSelectedEvent] = useState<(typeof events)[number] | null>(null)

  return <section id="eventos" className="bg-background px-6 py-24 lg:px-10 lg:py-32"><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="eyebrow">Agenda local</p><h2 className="section-title">Lo que pasa<br /><em>cerca de aquí.</em></h2></div><p className="max-w-xs text-sm leading-6 text-muted-foreground">Planes sencillos para descubrir el Valle de los Pedroches como alguien de aquí.</p></div><div className="mt-14 grid gap-5 md:grid-cols-2">{events.map((event) => <article key={event.title} className="group flex h-full flex-col overflow-hidden rounded-2xl border border-border bg-card"><div className="relative aspect-[16/8] overflow-hidden"><Image src={event.image} alt={event.imageAlt} fill className="object-cover transition-transform duration-500 group-hover:scale-105" /><div className="absolute left-4 top-4 flex items-baseline gap-2 rounded-full bg-background/95 px-3 py-2"><span className="font-serif text-2xl leading-none">{event.date}</span><span className="text-[10px] font-semibold tracking-widest text-accent-foreground">{event.month}</span></div></div><div className="flex flex-1 flex-col gap-4 p-5 sm:p-6"><div><p className="text-xs font-semibold uppercase tracking-wider text-accent-foreground">{event.category}</p><h3 className="mt-1 font-serif text-2xl">{event.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{event.description}</p></div><div className="mt-auto flex flex-wrap items-center justify-between gap-3 border-t border-border pt-4"><div className="flex items-center gap-2 text-xs text-muted-foreground"><MapPin className="size-4 text-accent-foreground" />{event.place}</div>{event.registrationEmbedUrl ? <button type="button" onClick={() => setSelectedEvent(event)} className="inline-flex items-center rounded-full bg-primary px-4 py-2.5 text-xs font-semibold text-primary-foreground transition-colors hover:bg-accent hover:text-primary">{event.registrationLabel ?? 'Reservar plaza'} <ArrowUpRight className="ml-1 size-4" /></button> : <a href={event.registrationUrl} target="_blank" rel="noreferrer" className="inline-flex items-center rounded-full bg-primary px-4 py-2.5 text-xs font-semibold text-primary-foreground transition-colors hover:bg-accent hover:text-primary">{event.registrationLabel ?? 'Más información'} <ArrowUpRight className="ml-1 size-4" /></a>}</div></div></article>)}</div><p className="mt-5 text-xs text-muted-foreground">¿Organizas un evento? Añádelo editando una entrada en <code className="rounded bg-secondary px-1.5 py-0.5 text-foreground">data/events.ts</code>: fecha, foto, descripción y enlace. Para reservar sin salir de Lantigua, añade también <code className="rounded bg-secondary px-1.5 py-0.5 text-foreground">registrationEmbedUrl</code>.</p></div>{selectedEvent && <div role="dialog" aria-modal="true" aria-labelledby="event-dialog-title" className="fixed inset-0 z-50 flex items-center justify-center bg-primary/70 p-4" onClick={() => setSelectedEvent(null)}><div className="flex h-[min(760px,90vh)] w-full max-w-4xl flex-col overflow-hidden rounded-2xl bg-background shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><p className="text-xs font-semibold uppercase tracking-widest text-muted-foreground">Inscripción</p><h2 id="event-dialog-title" className="font-serif text-2xl">{selectedEvent.title}</h2></div><button type="button" aria-label="Cerrar inscripción" onClick={() => setSelectedEvent(null)} className="rounded-full p-2 text-muted-foreground hover:bg-secondary hover:text-foreground"><X /></button></div><iframe title={`Inscripción a ${selectedEvent.title}`} src={selectedEvent.registrationEmbedUrl} className="min-h-0 flex-1 bg-background" /> <div className="flex items-center justify-between gap-4 border-t border-border px-5 py-3 text-xs text-muted-foreground"><span>Si el formulario no carga, puedes abrirlo en una nueva pestaña.</span><a href={selectedEvent.registrationUrl} target="_blank" rel="noreferrer" className="shrink-0 font-semibold text-foreground underline underline-offset-4">Abrir página</a></div></div></div>}</section>
}

export function Footer() { return <footer className="bg-secondary px-6 py-12 lg:px-10"><div className="mx-auto flex max-w-6xl flex-col gap-8 md:flex-row md:items-end md:justify-between"><div><div className="font-serif text-3xl">lantigua<span className="text-accent">.</span></div><p className="mt-3 text-sm text-muted-foreground">Valle de los Pedroches, Córdoba</p></div><div className="flex items-center gap-5 text-sm text-muted-foreground"><a href="mailto:hola@lantigua.es" className="hover:text-foreground">hola@lantigua.es</a><span className="text-accent-foreground">@lantigua.casarural</span><span>© 2026</span></div></div></footer> }
